import { SerachBoxPipe } from './serach-box.pipe';

describe('SerachBoxPipe', () => {
  it('create an instance', () => {
    const pipe = new SerachBoxPipe();
    expect(pipe).toBeTruthy();
  });
});
